class FilmeModel {
    constructor (id, nome, tipo, genero, descricao, lancamento) {
            this.id = id;
            this.nome = nome;
            this.tipo = tipo;
            this.genero = genero;
            this.descricao = descricao;
            this.lancamento = lancamento;
        }
    }

    export default FilmeModel;